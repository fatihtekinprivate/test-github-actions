name: test-github-actions-codeowner
on:
  [pull_request]

jobs:
  code-owner:
    name: code-owner
    runs-on: ubuntu-latest
    steps:
      - name: code-owner
        run: |
          echo "code owner should not run"

